<% if resource.errors.any? %>
  <div class="alert alert-warning">
    <h5><%= resource.errors.count %>件のエラーがあります</h5>
    <ul>
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
